import React from 'react';

const Map = (props) => {
    
    return (
        <div class="container">
            <h1>MAPA: {props.lat} & {props.long}</h1>
        </div>
    );
}

export default Map;
